using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TankSensorInfo : SensorInfo
{
   // public ConfigAgent agent;
    // Start is called before the first frame update
    public void Awake()
    {
      //  agent = GetComponent<TankAgent>();
       // teamId = agent.teamId;
    }

    //public override bool IsEnemy(ConfigAgent ag)
    //{
    //    if (agent == null || ag == null)
    //        return false;
    //    return (teamId != ag.teamId);
    //}

    
}
